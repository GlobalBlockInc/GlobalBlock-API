name: AI Deployment Optimizer

on: [push]

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Optimize Performance
        uses: ko-build/setup-ko@v0.5 
